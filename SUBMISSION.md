# Submission Form Values

## Name of the assignment you are submitting

**Airbnb Web App — SDE Fullstack Assignment**

## GitHub Link

Paste your public repository URL after pushing the project, for example:

`https://github.com/<your-username>/staybnb-fullstack`

## Vercel / Netlify / Render / Railway or any cloud service

Paste the deployed frontend URL, for example:

`https://staybnb-fullstack.vercel.app`

The frontend should call your deployed backend using `NEXT_PUBLIC_API_URL`.

## Assumptions / Mocked Data / Notes

Use this text in the form:

> The application uses seeded demo data stored in SQLite, including guest/host users, property listings, amenities, reviews, and existing bookings. Authentication is intentionally simplified through a Guest/Host role switcher. Payment is fully mocked and no real card transaction occurs. Listing images use public Unsplash URLs. Search, filters, favorites, listing CRUD, date-overlap validation, booking persistence, My Trips, and the host dashboard are functional. Messaging, identity verification, and live map pricing pins are shown only as out-of-scope/placeholder concepts, as permitted by the assignment.

## Original work declaration

The assignment explicitly permits AI-assisted development. Check the declaration **only after you have reviewed the project, run it yourself, understood the important code paths, and can explain the implementation during the interview**.
